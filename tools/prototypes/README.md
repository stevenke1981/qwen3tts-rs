# Prototype Tools

This directory contains temporary Python probes used while comparing upstream
Qwen3-TTS behavior with the Rust/Candle implementation.

Do not add new runtime dependencies here. Production conversion, fixture export,
and validation scripts should live directly under `tools/` with descriptive
names.
